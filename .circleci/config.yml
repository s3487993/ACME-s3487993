version: 2.1
commands:
  install_deps:
    steps:
      - run:
          name: Install Dependencies
          commands: |
            cd src
            npm install
jobs:
  build:
    docker:
      - image: circleci/node:lts

    steps:
      - checkout
      - install_deps

workflows:
  version: 2
  build-test-package:
    jobs:
      - build